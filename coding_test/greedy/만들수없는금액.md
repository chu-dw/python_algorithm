# 만들 수 없는 금액
## 문제
N개의 동전 가지고있음, N개의 동전을 가지고 만들 수 없는 양의 정수 금액중 최값을 구하라
예를들어 N=5이고, 각 동전이 각각 3원 2원 1원 9원이라 가정한다.
이떄 만들수 없는 양의 정수 금액중 최소는 8원이다.

## 입력
첫째 줄에는 동전의 개수를 나타내는 양의 정수N (1 < N < 1,000)
둘째 줄에는 각 동전의 화폐 잔위를 나타내는 N개의 자연수가 주어지며, 각 자연수는 공백으로 구분
이때 각  화폐의 단위는 1,000,000 이하의 자연수

## 출력
첫째줄에 주어진 동전들로 만들 수 없는 양의 정수 금액중 최솟값을 

## 풀이
접근방법이 떠오르징않아 약간의 힌트를 받았다.
- 지금까지 만들 수 있는 금액의 범위”를 관리한다
바로 안떠올랐던 이유는 1~x의 연속된 수가 있으면 이 수를 다더한 값까지의 연속된 수를
들수 있다는걸 못떠올려서 였던거 같다.
즉 1,2,3,4 가있으면 1+2+3+4=10이므로 1부터 10까지 모든 수를 만들수 있다.
이를 가지고 코드를 구현했다.
```python
N = int(input())
coins = list(map(int,input().split()))

coins.sort()

temp_range = 0

for coin in coins:
  print(coin, temp_range)
  
  if coin <= temp_range+1:
    print('연속가능')
    temp_range += coin
    print(temp_range)

  else:
    print('연속 불가능')

  print('---')
``` 
여기서 더 헷갈리느 부분은 연속 가능한 범위를 범위로 저장하려고 했다.
그래서 배열로 관리하려하니 꼬였다.
간단하게 
